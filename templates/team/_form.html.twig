{{ form_start(form) }}



<div class="row">
    <div class="form-group col-4">
        <div>
            <label>Team name</label>
        </div>
        {{ form_widget(form.name,{'attr': {'placeholder': 'Insert team name','style':'width:100%'}}) }}
    </div>
    <div class="form-group col-1" style="float:left;padding-top: 25px;">
        <button class="btn btn-outline-success">{{ button_label|default('Save') }}</button>
    </div>
</div>
{{ form_end(form) }}
